# Gradle / build output
.gradle/
build/
captures/

# Native build (NDK / CMake)
.cxx/
.externalNativeBuild/

# Vendored native dependencies, fetched by CMake at configure time
/app/src/main/cpp/opencv
/app/src/main/cpp/openh264

# Local machine config — SDK and NDK paths differ per checkout
/local.properties

# IDE
.idea/
.kotlin/
*.iml
.cursor

# Signing material. Never commit these.
*.jks
*.keystore
keystore.properties

# Packaged artifacts
*.apk
*.aab
*.ap_
*.dex

# Camera and daemon capture dumps. `*.png` is blanket-ignored because dumps
# land as PNG anywhere in the tree; committed raster art is WebP for this
# reason as much as for size.
/scratch
ref/
design-lab/
dilink5_camera_dump*
*.raw
*.zip
*.png

# OS
.DS_Store
Thumbs.db

# Pulled device data and local scratch dirs
ref
device_backup
logs_debug
downloaded_logs
frame_grabber_light
