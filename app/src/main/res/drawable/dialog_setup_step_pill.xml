<?xml version="1.0" encoding="utf-8"?>
<!-- Step-number badge in the setup guide. Rounded square, not a circle, so the
     badges read as part of the same shape family as the cards around them. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorPrimaryContainer" />
    <corners android:radius="@dimen/card_radius_accent" />
</shape>
