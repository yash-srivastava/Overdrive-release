<?xml version="1.0" encoding="utf-8"?>
<!--
  Read-only panel nested inside a dialog. surfaceContainer sits one tier below
  the surfaceContainerHigh that Theme.Overdrive.M3.Dialog gives the window, so
  the panel recesses into the dialog instead of matching it; the hairline keeps
  the edge readable in light mode, where the two tiers are close.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurfaceContainer" />
    <stroke
        android:width="1dp"
        android:color="?attr/colorOutlineVariant" />
    <corners android:radius="@dimen/dashboard_modern_radius" />
</shape>
